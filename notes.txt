babel: new js -> old js

webPack: mudule bundler

npx create-react-app appname to create app

npm start to start

npm run build makes a production ready build

//if we delete the node modules file then then with the command "npm install" it'll re install all the dependencies!
//list of our dependencies are already inside the package.json file

//installed prettier format

//search format: format in paste. format in save

//devtools->compnents( will be able to see what components are being used